<p style="color: green"><%= notice %></p>

<h1>Products</h1>

<div id="products" class="row flex-wrap">
  <% @products.each do |product| %>
    <div class="col-md-6 my-1">
    <%= link_to product do%>
      <%= render product %>
    <% end %>
    </div>
  <% end %>
</div>

<%= link_to "New product", new_product_path %>
